{
  "name": "package-sync",
  "main": "./lib/package-sync",
  "version": "0.0.2",
  "description": "Synchronizes packages installed between computers",
  "activationEvents": [
    "package-sync:sync"
  ],
  "repository": "https://github.com/lee-dohm/package-sync",
  "license": "MIT",
  "engines": {
    "atom": ">0.50.0"
  },
  "dependencies": {
    "season": "~1.0"
  }
}
